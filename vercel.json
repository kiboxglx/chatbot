{
    "outputDirectory": "frontend/dist",
    "installCommand": "cd frontend && npm install",
    "buildCommand": "cd frontend && npm run build",
    "rewrites": [
        {
            "source": "/api/:path*",
            "destination": "https://chatbot-production-e324.up.railway.app/:path*"
        },
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}